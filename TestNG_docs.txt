TestNG(Next Generation)


-It is an open source testing framework inspired by Junit
-provides following useful features:

1. Annotations           -used with functions
2. Grouping              -   
3. Sequencing / prioritization /dependency(any method depends on another) 
4. Parameterization      -@parameters, @DataProviders
5. Parallel Testing/Cross Browser Testing
6. Reports


Annotations

@Test
@BeforeMethod
@AfterMethod
@BeforeTest
@AfterTest
@BeforeClass
@AfterClass
@BeforeGroup
@AfterGroup
@BeforeSuite
@AfterSuite
@Parameters
@DataProviders

by default it execute sequentially (numerical/alphabetical)

Sequencing
@Test(priority=0)
public func(){
{

dependsOnGroup
dependsonmethod

@Test(dependsOnMethods={"OpenBrowser"})

@Test(dependsOnGroups={"OpenBrowser"})

@Test(Priority=4, dependsOnMethods={"a"})

dependency>Priority
@Test(invocationCount=3)
looping the execution

@Test(enabled=false)
@Test(groups={"A"})


suite
test
groups
run 
include
classes




